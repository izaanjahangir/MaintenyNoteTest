import {StyleSheet} from 'react-native';
import {THEME_WHITE} from '../../theme/colors';
import {SPACING_SMALL} from '../../theme/measurements';

export default StyleSheet.create({
  container: {
    height: 1,
    backgroundColor: THEME_WHITE,
    marginVertical: SPACING_SMALL * 1.5,
  },
});
