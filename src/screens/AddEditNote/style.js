import {StyleSheet} from 'react-native';
import { SPACING_MEDIUM } from '../../theme/measurements';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  saveButtonContainer: {
    marginTop: SPACING_MEDIUM
  }
});
