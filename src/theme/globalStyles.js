import {StyleSheet} from 'react-native';

import {SPACING_MEDIUM} from './measurements';

export default StyleSheet.create({
  container: {
    paddingHorizontal: SPACING_MEDIUM,
    paddingVertical: SPACING_MEDIUM,
    flex: 1,
    justifyContent: "center"
  },
});
